{% extends 'base.html.twig' %}

{% block title %}RendezVous{% endblock %}

{% block body %}
<h1>show rendez-vous</h1>
<div class="flex justify-center items-center h-screen bg-blue-200">
    <div class="w-full max-w-md bg-gray-200 p-4 rounded-md">
            <div class="mb-4">
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rendez_vou.id }}</td>
            </tr>
            <tr>
                <th>DateR</th>
                <td>{{ rendez_vou.dateR ? rendez_vou.dateR|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>NomService</th>
                <td>{{ rendez_vou.nomService }}</td>
            </tr>
            <tr>
                <th>Statut</th>
                <td>{{ rendez_vou.statut ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>
            </div>
            <div class="mb-4">      
    <a href="{{ path('app_rendez_vous_index') }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">back to list</a>
            </div>
            <div class="mb-4">
    <a href="{{ path('app_rendez_vous_edit', {'id': rendez_vou.id}) }}" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded w-full">edit</a>
    </div>
    <div class="mb-4"></div>
    {{ include('rendez_vous/_delete_form.html.twig') }}
</div>
</div>
</div>
    {% endblock %}
